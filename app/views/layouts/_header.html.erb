<header class="navbar navbar-fixed-top navbar-inverse">
   <div class="navbar-inner">
     <div class = 'head-mar'>
       <nav>
         <ul class="nav pull-left">
           <li>
             <div class = 'logo-box'>
               <%= link_to image_tag('logo.png'), root_path %>
             </div>
           </li>
           <li>
           <%= form_tag(nodes_path, :method => "get", id: "search-form") do %>
               <div class = 'search-container'>
                   <div class ='search-bar-container'>
                     <%= text_field_tag :search, params[:search], placeholder: "Search Pulsers" %>
                   </div>
                     <%= submit_tag '?', name: nil, class: 'search_button' %>
             </div>
           <% end %>
           </li>
           </ul>
       </nav>
            <nav>
            <ul class="nav pull-right">
              <% if signed_in? %>
                    <li> <a href='/'> Home </a> </li>
                    <li> <%= link_to 'Assemblies', view_path(:id => current_node.id) %></li>
                    <!--<li><%= link_to 'My Profile', current_node %></li>-->
                    <li> <%= link_to inbox_path do %>
                          <i class='fa fa-small fa-envelope'></i>
                          <% if current_node.unreads.count > 0 %>
                          <span class="badge badge-important"><%= current_node.unreads.count %></span>
                          <% end %>
                      <% end %>
                    </li>
                  <!--<li id='fat-menu' class='dropdown'>-->
                    <!--<a href='#' class='dropdown-toggle' data-toggle='dropdown'>-->
                      <!--</a>-->
                    <!--<ul class='dropdown-menu'>-->
                      <li><%= link_to edit_path do%><i class="fa fa-large fa-cog"></i><%end%></li>
                  <li><%= link_to inputs_path(:id => current_node.id) do%><i class="fa fa-large fa-users"></i><%end%></li>
                      <!--<li class="divider"></li>-->
                      <li>
                        <%= link_to 'Pulse Out', pulseout_path, :method => 'delete' %>
                      </li>
                    </ul>
                  </li>
              <% else %>
                  <li><%= link_to 'Pulse In', pulsein_path %></li>
                  <li><%= link_to 'Home', root_path %></li>
              <% end %>
            </ul>
          </nav>
        </div>
      </div>
  </div>
</header>